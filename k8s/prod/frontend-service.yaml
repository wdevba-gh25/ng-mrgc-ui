apiVersion: v1
kind: Service
metadata:
  name: emergency-surge-frontend
  namespace: aks-mrgnc-surge-prod
spec:
  selector:
    app: emergency-surge-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer